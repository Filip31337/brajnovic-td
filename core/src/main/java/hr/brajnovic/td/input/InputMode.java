package hr.brajnovic.td.input;

/** Prepared for V3 Android/touch support; MOUSE drives the current desktop-only build.
 * TOUCH can already be selected in Options to simulate/dogfood the touch UX with a mouse on desktop. */
public enum InputMode {
    MOUSE,
    TOUCH
}
